# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Player Task' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Player Task
  
  pod 'Firebase/Auth'
  pod 'GoogleSignIn'
  pod 'SDWebImage', '~> 5.0'
  target 'Player TaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Player TaskUITests' do
    # Pods for testing
  end

end
